3. Avoid partial streamed execution: download the complete script into memory
   before running it:[106;5u[106;5u
   installer=$(curl -fsSL URL) &&[106;5u     sh -c "$installer"[106;5u[106;5u
   This keeps the no-file workflow but does not execute bytes while curl is
   still transferring. Curl can otherwise report a partial-transfer error only
   after some content[106;5u has entered the pipe. curl documentation
   (https://curl.se/docs/manpage.html)[106;5u[106;5u 4. Make retries safe:
   detect an existing user instead of failing at adduser, and replace managed
   configuration blocks instead of appending them repeatedly. Existing
   users[106;5u should require explicit confirmation before receiving
   passwordless sudo.[106;5u[106;5u
